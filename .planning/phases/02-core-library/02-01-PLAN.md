---
phase: 02-core-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib.rs
  - src/error/mod.rs
  - src/error/codes.rs
  - Cargo.toml
  - src/features/mod.rs
  - src/features/std.rs
  - src/features/no_std.rs
autonomous: true
requirements:
  - ERROR-01
  - ERROR-02
  - ERROR-03
  - ERROR-04
  - PLAT-01
  - PLAT-02
  - PLAT-03
user_setup: []

must_haves:
  truths:
    - "Custom error enum compiles in both std and no_std environments"
    - "Error types implement core::error::Error (not std::error::Error)"
    - "Error types support defmt::Format derive when defmt feature enabled"
    - "std feature enables Tokio and standard library"
    - "alloc feature enables heap-based collections"
    - "no_std build compiles successfully with --no-default-features"
  artifacts:
    - path: "src/error/mod.rs"
      provides: "NexoError enum with Display and Error trait impls"
      min_lines: 80
    - path: "src/error/codes.rs"
      provides: "Error code constants and categories"
      min_lines: 40
    - path: "src/features/mod.rs"
      provides: "Feature flag module organization"
      min_lines: 20
    - path: "Cargo.toml"
      provides: "Feature flag definitions (std, alloc, defmt)"
      contains: "[features]"
  key_links:
    - from: "src/lib.rs"
      to: "src/error/mod.rs"
      via: "pub use error::NexoError"
      pattern: "pub use error::"
    - from: "src/error/mod.rs"
      to: "core::error::Error"
      via: "impl Error for NexoError"
      pattern: "core::error::Error"
    - from: "Cargo.toml [features]"
      to: "PLAT-01 requirement"
      via: "std = [] feature definition"
      pattern: 'std = \['
---

<objective>
Implement no_std-compatible error types and establish platform feature flag architecture.

Purpose: Error handling is foundational - all other layers (codec, validation, transport) depend on error types. Feature flags must be established early to ensure no_std compatibility is tested from the start, preventing std leakage that would block bare-metal targets.

Output: Custom NexoError enum working in both std and no_std, feature flags for std/alloc/defmt, ready for codec and validation layers to depend on.
</objective>

<execution_context>
@/Users/prathanbomb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prathanbomb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-library/02-RESEARCH.md
@src/lib.rs
@Cargo.toml

# Phase 1 Summary - What We Built
From .planning/phases/01-schema-conversion/01-03-SUMMARY.md:
- 18 proto files (common.proto + casp.001-017.proto) successfully converted
- Generated code: src/protos/nexo.casp.v1.rs (4575 lines, 176 types)
- BTreeMap configured for no_std compatibility in build.rs
- All message types available at crate root via include! macro
- FIELD_REGISTRY.md tracks all field assignments with reserved number tracking

# Existing Code Structure
From src/lib.rs:
```rust
#![cfg_attr(not(feature = "std"), no_std)]

//! # Nexo Retailer Protocol (ISO 20022 CASP)
//! ...

include!("protos/nexo.casp.v1.rs");

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn test_library_builds() {
        assert!(true, "library builds successfully");
    }
}
```

From Cargo.toml:
```toml
[features]
default = ["std"]
std = ["prost/std"]
alloc = []
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement no_std-compatible error types</name>
  <files>
    src/error/mod.rs
    src/error/codes.rs
  </files>
  <action>
Create error module structure with custom NexoError enum. Follow the pattern from RESEARCH.md section "Pattern 4: no_std Compatible Error":

1. Create src/error/mod.rs with:
   - NexoError enum with variants: Connection, Timeout, Validation, Encoding, Decoding
   - Each variant has appropriate fields (details: &'static str for most, specific fields for Validation)
   - #[cfg_attr(feature = "defmt", derive(defmt::Format))] for embedded logging support (ERROR-04)
   - impl fmt::Display with match arms providing meaningful error messages (ERROR-02)
   - impl core::error::Error (not std::error::Error) with source() returning None (ERROR-03)
   - impl From<prost::DecodeError> and impl From<prost::EncodeError> for easy conversion

2. Create src/error/codes.rs with:
   - Error code constants organized by category
   - Module documentation explaining error code taxonomy
   - Re-export in mod.rs

Do NOT use thiserror crate (std-only). Manual Error impl is required for no_std per RESEARCH.md.

Per RESEARCH.md "Anti-Patterns to Avoid": Do NOT use std::error::Error, must use core::error::Error.
  </action>
  <verify>
    cargo build --no-default-features succeeds
    cargo build --features std succeeds
    cargo test --no-default-features passes (error types compile and trait impls valid)
  </verify>
  <done>
NexoError enum compiles in both std and no_std, implements core::error::Error, has defmt Format derive gated on feature, Display impl provides meaningful messages, From impls for prost errors exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Establish platform feature flag architecture</name>
  <files>
    Cargo.toml
    src/features/mod.rs
    src/features/std.rs
    src/features/no_std.rs
    src/lib.rs
  </files>
  <action>
Create feature flag infrastructure per RESEARCH.md "Pattern 5: Feature Flag Architecture":

1. Update Cargo.toml [features] section:
   - Keep existing: default = ["std"], std = ["prost/std"], alloc = []
   - Add: defmt = ["dep:defmt"] (conditional dependency)
   - Add to [dependencies]: defmt = { version = "0.3", optional = true }
   - Add to [dependencies]: heapless = { version = "0.8", optional = true }

2. Create src/features/mod.rs:
   - Module documentation explaining feature flag philosophy (additive principle)
   - Re-exports of platform-specific implementations

3. Create src/features/std.rs (#[cfg(feature = "std")]):
   - Std-specific implementations when needed (placeholder for now, used in Phase 3+)

4. Create src/features/no_std.rs (#[cfg(not(feature = "std"))]):
   - no_std-specific implementations (placeholder for now, used in Phase 3+)

5. Update src/lib.rs:
   - Add: pub mod error;
   - Add: pub mod features;
   - Ensure feature-gated modules are properly conditioned

Per RESEARCH.md: Use #![cfg_attr(not(feature = "std"), no_std)] at top of lib.rs (already present).
Use #[cfg(feature = "alloc")] extern crate alloc; when needed for heap collections (VAL-06).
  </action>
  <verify>
    cargo build --no-default-features succeeds (pure no_std)
    cargo build --features std,alloc succeeds (full std)
    cargo build --features defmt succeeds (defmt logging available)
    cargo tree --target thumbv7em-none-eabihf --no-default-features shows no std dependencies
  </verify>
  <done>
Feature flags defined in Cargo.toml, features module structure created, lib.rs exports error and features, no_std build has no std leakage, defmt feature enables conditional derive.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add error module integration and documentation</name>
  <files>
    src/lib.rs
    src/error/mod.rs
  </files>
  <action>
Integrate error types into library public API and document usage:

1. Update src/lib.rs:
   - Add pub use error::{NexoError, ValidationError, EncodeError, DecodeError};
   - Add module-level documentation for error types with examples
   - Add #[cfg(feature = "std")] std-specific error convenience methods (e.g., impl std::error::Error for NexoError when std feature enabled)

2. Update src/error/mod.rs:
   - Add comprehensive documentation for each error variant
   - Add usage examples in module doc comments
   - Document the no_std compatibility approach

3. Add validation error types for use in Phase 02-03:
   - ValidationError enum with variants: MissingRequiredField, InvalidCurrency, StringTooLong, NanosOutOfRange, etc.
   - Each variant has fields providing context (field name, expected vs actual values)
   - impl Display for ValidationError
   - impl core::error::Error for ValidationError

Reference RESEARCH.md "Common Pitfalls" section for error scenarios to cover.
  </action>
  <verify>
    cargo doc --no-deps --document-private-items succeeds (documentation compiles)
    cargo build succeeds with all error types exported
    User can import: use nexo_retailer_protocol::{NexoError, ValidationError};
  </verify>
  <done>
Error types exported at crate root, documentation covers all variants and usage patterns, ValidationError type ready for validation layer, examples demonstrate no_std usage.
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
1. cargo build --no-default-features succeeds (pure no_std compilation)
2. cargo build --features std,alloc,defmt succeeds (full feature compilation)
3. cargo tree confirms no std leakage in no_std build
4. All error types implement core::error::Error (not std::error::Error)
5. defmt::Format derive only active when defmt feature enabled
</verification>

<success_criteria>
1. NexoError enum with 5+ variants (Connection, Timeout, Validation, Encoding, Decoding)
2. core::error::Error implemented (compile proves no std dependency)
3. Feature flags: std, alloc, defmt defined and additive
4. No std:: imports in error code
5. ValidationError type with field-specific variants ready for Phase 02-03
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-library/02-01-SUMMARY.md`
</output>
